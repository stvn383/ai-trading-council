[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-council"
version = "0.1.0"
description = "Multi-agent AI paper trading council"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]